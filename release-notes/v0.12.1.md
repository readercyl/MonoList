# MonoList v0.12.1

本版本修正升级后主页窗口可能沿用旧版超宽尺寸的问题，让主页恢复为更适合轻量提醒工具的竖向比例。

## 更新内容

- 主页默认窗口调整为 `400 × 720 pt`，高度保持不变，设置页内容仍可完整展示。
- 升级后首次打开会使用新的紧凑窗口记录，不再继承旧版本遗留的超宽窗口尺寸。
- 用户之后手动调整的窗口大小仍会被 macOS 记住。

## 兼容性

- 支持 Apple 芯片 Mac（M1 或后续型号）。
- 支持 macOS 14.0 及以上版本。
- 不改变待办、完成记录、提醒设置或本地数据格式。
